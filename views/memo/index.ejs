<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h3><%= title %> <a href="/memo/add">追加</a></h3>
    <div>
      <table>
        <% for(var i in content) { %>
        <tr>
          <% var obj = content[i]; %>
          <td><%= obj.id %></td>
          <td><%= obj.text %></td>
          <td><%= obj.createdAt %></td>
        </tr>
        <% } %>
      </table>
    </div>
  </body>
</html>
